# 모듈 패키징과 소비 인터페이스

Status: open
Labels: wayfinder:grilling
Assignee:
Blocked-by: 05, 06

## Question

추출된 코어는 어떤 형태로 존재하는가(독립 레포? 파이썬 패키지? 데이터 익스포트 CLI?) 그리고 정적 웹 게임 빌드는 이를 어떻게 소비하는가(빌드타임 JSON 익스포트 등 — Python 코어 ↔ JS 게임의 언어 경계 처리). 여기서 결정된 절차가 일반화 추출 플로우 스킬의 골격이 된다.
