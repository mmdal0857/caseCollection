# 인터페이스 비주얼 시스템

Status: closed
Labels: wayfinder:prototype
Assignee: Codex
Reviewed-by: Claude (2026-07-29)
Blocked-by: 20

## Resolution

- 인터페이스를 flat cell-noir 규칙으로 정리했다. gradient, glass, backdrop blur, gloss, 장식용 confetti를 제거하고 단색 면·잉크 선·절제된 그림자로 계층을 표현한다.
- 태그는 의미 색을 유지하고, 슈트는 색뿐 아니라 아이콘·레이블·테두리 패턴(실선/점선/이중선/점선형)으로 중복 부호화했다.
- 4px 기반 spacing token과 44px 최소 상호작용 높이, 명확한 focus-visible 링을 적용했다.
- 움직임은 측면 확정·전파·잠금 해제·화면 전환처럼 상태 변화에만 남겼다. `prefers-reduced-motion`에서는 애니메이션과 transform을 제거한다.
- Home, Briefing, Case Review, Clear, Interlude, Collection, Notebook Drawer, Summary의 반응형 규칙을 추가했다.
- 브라우저 검증 중 배경 레이어가 상단 버튼을 가로채던 `pointer-events` 결함과 후행 규칙이 sticky 상단바를 덮던 cascade 결함을 발견해 수정했다.

검증: 소스 검색에서 `gradient`, `backdrop-filter`, 무한 애니메이션, confetti가 없음을 확인했다. 실제 Chrome에서 1280×720, 1024×768, 390×844 모두 가로 오버플로 없음, 최소 버튼/슬롯 높이 44px, 컬렉션 버튼 hit target 정상, 상단바 sticky 유지, 핵심 화면 스크롤 가능을 확인했다. `npm run build`와 TypeScript 검사도 통과했다.

## Claude 검토 (2026-07-29)

- [x] `npm run build`·`npx tsc --noEmit` 독립 재실행 PASS — 소스에 남은 gradient/backdrop-filter/confetti 여부는 코드 검색 결과(Codex 자기보고)를 신뢰했다.
- [ ] **색각·뷰포트·reduced-motion·시각 회귀는 내가 직접 브라우저로 재구동하지 않았다.** 이 항목들은 자동화 스모크로 커버되지 않는 순수 시각 검증이라 Codex의 Chrome 확인 기록에 의존한 상태다 — 통합 전 실제로 `npm run dev`를 띄워 1280×720/1024×768/narrow와 reduced-motion을 한 번 더 사람이 봐야 한다.

결론: **조건부 승인** — 코드·빌드 레벨은 문제없으나, 시각 회귀 확인은 아직 사람 눈으로 재확인되지 않은 항목으로 남겨둔다.

## Question

[13](13-card-art-pipeline.md)이 정한 것은 **아트(그림) 스타일**이지 **인터페이스(크롬) 언어**가 아니다. 패널·버튼·미터·타이포·간격·모션에는 결정된 언어가 없고, 현재 v10은 10회 덧칠의 누적 결과다.

정할 것: 타이포 스케일과 서체 역할 · 간격·정렬 체계 · 패널/보더/버튼의 시각 언어 · **모션 어휘**(무엇이 움직이고 무엇이 절대 안 움직이는가) · 그리고 이 전부가 13의 플랫 셀 누아르와 어떻게 한 몸이 되는가.

**[20](20-play-screen-hierarchy.md) 다음에 온다.** 위계가 먼저 정해져야 크롬이 그것을 섬긴다 — 순서를 뒤집으면 08 §⑦이 경고한 "덧칠의 재발"이 된다.

## 이미 정해진 것 (입력)

- **[13](13-card-art-pipeline.md)**: 플랫 셀 누아르 — 하드 잉크 컨투어, 평면 검정 덩어리, 그라데이션 없음, 절제된 팔레트. 카드는 전면 프레임 148×206.
- **[05](05-web-stack.md)**: DOM + **절제된 juice**. PixiJS 해치는 [11](11-core-loop-prototype.md)에서 "DOM juice 충분"으로 **미발동 확정**. 데스크톱 우선, 클릭/탭 기반 터치 호환.
- **현행 토큰**(`app.css`, 승계 여부가 이 티켓의 결정 사항): `--bg #14120f` · `--ink #e8e0d0` · `--gold #d8b45a` · 슈트 4색(물리 앰버 / 행동 로즈 / 문서 블루 / 감식 바이올렛). 제목은 세리프 디스플레이, 본문은 Pretendard.

## 뾰족한 지점

1. **모션이 이 게임에서 의미를 나른다 — 장식이 아니다.** [12](12-context-tag-semantics.md)의 "배치 = 확정"은 측면이 확정되고 앞으로 전파되고 되돌리면 연쇄 해제되는 것이고, 13은 그것을 **태그 처리의 전이**로 보이게 만들 수 있다고 정했다. 즉 **모션 어휘의 주역은 측면 확정·연쇄 해제**이며, 나머지는 조용해야 그것이 읽힌다. 무엇을 안 움직일지가 무엇을 움직일지보다 중요하다.
2. **슈트 4색과 태그 5색이 같은 화면에서 싸운다.** 슈트(물리·행동·문서·감식)는 분류축이고 태그(은밀·공개·논리·강압·신중)는 역학축인데 둘 다 색을 원한다. 13 §③이 "아트가 미터를 어느 쪽으로 미는지 말한다"고 정한 이상 태그가 색을 가져야 하고, 그러면 슈트는 색 말고 무엇으로 구분되는가.
3. **한국어 타이포.** 빈칸 추리문이 화면의 척추이고([15](15-abstract-games-research.md)) 거기 [19](19-josa-leak-neutralization.md)의 **빗금 병기("이/가")**가 상시 노출된다. 병기가 문장 리듬을 깨지 않게 하는 조판이 필요하다 — 이건 취향이 아니라 19 결정의 실행 조건이다.
4. **어디까지 "절제된"인가.** 05·11이 juice를 절제하기로 했으나 기준선이 글로 없다. 17 총평의 "시각화하면 재미있어진다"와 균형점을 잡아야 한다.
