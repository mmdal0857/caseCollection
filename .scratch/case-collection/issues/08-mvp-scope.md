# MVP 스코프 (8월 마일스톤)

Status: open
Labels: wayfinder:grilling
Assignee:
Blocked-by: 03, 04

## Question

2026-08 첫 공개 빌드에 무엇이 들어가는가 — 카드 수, 메커닉 범위(코어 루프의 어느 변주까지), 아트 수준, 배포 채널. 마일스톤은 느슨한 제약이므로 스코프를 마일스톤에 맞추되 재미 검증(Koster)을 희생하지 않는 선을 긋는다.

## Comments

- 2026-07-23 스코프 방향 입력 — [17 프로토 v10](17-context-semantics-prototype.md) 플레이 총평(사용자): **"재미있다 — 핵심 코어를 유지하면서 퍼즐들을 붙여나가고 시각화하면 재미있는 게임이 될 수 있다."** 코어 루프·얼굴 의미론은 동결 대상, MVP 확장축은 ①퍼즐 콘텐츠 양(case 수·카드 수) ②시각화([13](13-card-art-pipeline.md))로 좁혀졌다.

- 2026-07-25 **그릴링 1회차 완료 — 8개 항목 확정, 1개 미확정. 티켓은 의도적으로 open 유지**(사용자 지시: "닫지 말고 다른 세션의 근간으로 유지"). 아래는 결의이지 Resolution이 아니다 — 지도 "Decisions so far" 등재도 보류 상태다. 후속 세션은 이 블록을 입력으로 삼되, 미확정 1건(콘텐츠 분량)이 [13](13-card-art-pipeline.md)의 산출을 기다린다는 점을 전제할 것.

  **① 목적 = 재미 검증이 아니라 토대.** 완료 조건은 "다음 스테이지로 넘어갈 수 있는 진짜 코드베이스 + 올려둔 빌드"다. 판정은 마켓플레이스 사용자 수 — 정밀 계측이 아니라 가벼운 관심 신호. **/ 기각**: 외부 관찰 플레이테스트(5~10명 대면), 로컬 계측+설문, 파이프라인 전 과정 관통, 상품성 완성. **/ 유예**: 리플레이성 검증([01](01-mechanic-survey.md)이 "최대 과제"로 지목) — 이번 빌드는 "한 판이 재미있는가"만 답하고 "계속 하고 싶은가"는 답하지 않는다는 것을 명시적으로 수용.

  **② 코드 토대 = 순수 모듈 승격 + UI 재작성.** `engine·facets·scenario·dramaturgy·josa·persona`(~1,500줄)와 `smoke`·`smoke-datapack` 검증(434줄)을 정식 소스로 승격, Svelte UI(~1,100줄)는 폐기하고 새로 짓는다. 근거: [17](17-context-semantics-prototype.md)이 "검증기의 실체"로 지목한 자산(`facetStatus`/`checkSolvable`/`checkCoherent`)을 버리면 A~E PASS라는 증명도 함께 버려지고 5주로 다시 살 수 없다. 반대로 UI는 v1~v10에서 10번 덧칠된 층이고 `ScenarioBoard`(166줄)처럼 [11](11-core-loop-prototype.md)에서 이미 기각된 모드가 남아 있다. 결정적 근거는 17 총평의 "시각화" — **앞으로 가장 많이 손댈 층이 정확히 UI이므로 거기가 가장 깨끗해야 한다.** **/ 기각**: 프로토 전체 승격(부채 승계), 전면 재작성(5주 내 배포 불가).
  - ⚠️ **미결 세부**: `datapack.ts`(472줄)+스키마+추출 스크립트를 승격 대상에 포함할지는 사용자 확인 전이다. 포함하면 [07](07-module-packaging.md)의 **분리빌드 뼈대가 MVP에 들어온다**(게임이 커밋된 JSON 팩을 읽어 빌드). 그릴링 중 권고는 "포함" — 콘텐츠를 `content.ts` 하드코딩으로 두면 나중에 반드시 뜯게 되므로. 단 ①에서 "파이프라인 관통은 목적 아님"으로 정했으므로 논리적 긴장이 있고, 이 판단은 [16](16-external-data-pack-loading.md) 작업 세션과 겹친다.

  **③ 메커닉 범위 = run 완주 + 컬렉션 영구화 축.** 얼굴 선택·이웃 전파·연쇄 해제·태그 다수결·드라마투르기 반응·가설 선언·수사 노트·인터루드 AP·보스·엔딩 전부 + 컬렉션 화면(보유×검증 2축)과 게스트 영구 획득. **/ 기각**: [10](10-acquisition-detail.md) 전량(수사 포인트 화폐·인터루드 진열 구매·완주팩 5중1·힌트 카드 경제) — 10은 지도 마커 `[종이]`이고 스스로 "수치 잠정, 프로토서 튜닝"이라 적었다. 미검증 경제를 UI까지 지어 올리는 것은 ①에서 배제한 종류의 리스크. 반면 영구화 축이 기대는 [04](04-card-schema.md)는 `[검증]`이다. **/ 기각**: 수집 없는 순수 퍼즐 — 게임 정체성이 마켓플레이스에서 안 보인다.

  **④ 콘텐츠 분량 = 미확정(유일한 열린 항목).** 잠정 24장(단서 20 + 패턴 4)으로 시작하고, **[13](13-card-art-pipeline.md)의 아트 코스트 실측 후 재측정**한다(사용자 지시). 논거: **49장 전량 저작은 사실상 [18](18-case-generator-shape.md)에 묶여 있다** — [12](12-context-tag-semantics.md)가 확정했듯 얼굴의 의미는 이웃이 만들고 배경 상태가 게이트하므로, case 4~5건뿐인 상태에서 49장분 얼굴 100~150개를 저작하면 대부분이 **어느 case에서도 켜지지 않는 죽은 얼굴**이 되고 삼중 제약 검증조차 걸 수 없다. 카드는 case가 늘 때 함께 늘어야 한다.
    - **크레딧은 제약이 아님(실측)**: Higgsfield 잔액 1,110 크레딧, 이미지 1장당 `z_image` 0.15 / `seedream_v5_lite`·`nano_banana_2_lite` 1 / `nano_banana_flash` 1.5 / `nano_banana_pro` 2 / `gpt_image_2` 7. 53장×4시도 기준 `nano_banana_pro`까지 424크레딧으로 잔액 내, `gpt_image_2`만 1,484로 초과. **진짜 코스트는 스타일 일관성을 잡는 반복 횟수와 사람 큐레이션 시간**이며 그 실측이 13의 소관.

  **⑤ 아트 수준 = 전부(카드 + 배경 + 인물 + UI 장식).** 단 **배경은 case가 아니라 배경 상태([12](12-context-tag-semantics.md) §5의 주목×신뢰 2고정축)에 귀속**한다 — 2축×3구간 9장, 축소 시 4~5장으로 판 전체를 덮는다. 이 귀속의 이득 3중: ①이미지 수가 case 수와 무관해져 **18이 case를 대량 생성해도 배경은 재사용**된다 ②태그 다수결에 따라 배경이 실제로 변하므로 v8의 "영향력 줄다리기 가시화"가 장식이 아니라 **정보를 나르는 그림**이 된다 ③17 총평의 "시각화하면 재미있어진다"가 가리키던 종류일 가능성이 높다. **/ 기각**: case별 전용 배경(18 이후 폐기), case_pattern 귀속(재사용되나 메커닉과 비연동).

  **⑥ 배포 = Higgsfield 마켓플레이스 + GitHub Pages 병행.** [05](05-web-stack.md)의 확정(itch.io butler + GitHub Pages)에 **없던 채널이므로 05에 파급 코멘트 필요**. itch.io는 MVP 밖. **알려지지 않은 변수(정직하게 기록)**: Higgsfield 마켓플레이스의 실제 관객 규모는 확인되지 않았다 — CLI에 목록·통계 커맨드가 없어 페이지를 직접 봐야 한다. "사용자 수로 판단"의 핵심 변수인데 그 값을 모른 채 결정한 것이므로, 신호가 0이면 itch.io 추가를 재검토할 것.

  **⑦ 순서 = 스타일 키 우선, 이후 병행.** [13](13-card-art-pipeline.md)에서 시안 비교로 **스타일 키 1~2장만 먼저 확정**(코스트 실측도 여기서 나온다) → 그 후 나머지 카드 생성과 코드 승격·UI 재작성을 병행. 근거: UI 재작성의 진짜 비용은 로직이 아니라 룩앤필이고, 스타일 미정 상태로 UI를 짜면 폴백 프레임 기준으로 만들었다가 다시 조정하게 된다 — ②에서 피하기로 한 "덧칠"의 재발.

  **⑧ MVP 밖**: [18](18-case-generator-shape.md) 생성기 · [14](14-tag-extraction-promotion.md) 태그 추출 · [16](16-external-data-pack-loading.md) 외부 팩 · 리플레이성 검증 · itch.io.

  **미처리 파급(닫을 때 실행할 것)**: ⓐ [05](05-web-stack.md)에 배포 채널 파급 코멘트 ⓑ **[13](13-card-art-pipeline.md)이 08의 하류가 아니라 입력으로 역전** — 13에 요구 추가 필요: 스타일 키 우선 / 코스트·반복횟수 실측 / 배경=상태 귀속 4~9장 / 레이든 표정 3~4종 / **마켓플레이스 커버 16:9·아이콘 1:1은 https URL 호스팅 필수**(`higgsfield game deploy --thumbnail/--favicon`, `publish --cover-url/--logo-url`이 https만 받음) ⓒ 08 자체의 ④는 13 close 시 재측정 결과를 코멘트로 랜딩.
