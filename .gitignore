# 의존성·빌드 산출물 — 어느 하위 디렉토리에서든
node_modules/
dist/

# 프로토타입 빌드 부산물
# (2026-07-23: prototype/core-loop는 main에 병합됨 — 더 이상 throwaway 브랜치 전용이 아니다.
#  npm install·esbuild가 남기는 산출물만 무시한다.)
prototype/core-loop/smoke.mjs
prototype/core-loop/smoke-datapack.mjs
prototype/core-loop/.vite/
