# caseCollection

퍼블릭 도메인 추리소설(OUT LLM 위키) 데이터를 기반으로, 수집한 단서 카드로 생성된 사건을 추리하는 웹 카드 게임의 도메인 언어.

## Language

### 사건과 진행

**case (사건)**:
빌드타임에 생성된 사건 퍼즐 1판 — case_pattern 골격에 story의 소재(인물·범죄유형·트롭)를 입힌 것. 플레이어가 단서 카드로 재구성하는 대상.
_Avoid_: 판, 스테이지, incident, mystery
※ OUT 프로젝트 용어집에서 "case"는 기피어(에피소드 인스턴스와 혼동)지만, 이 레포는 독립 컨텍스트로서 명시적으로 재정의해 채택한다 (코어 루프 확정 티켓, 2026-07-19).

**case_pattern (사건 패턴)**:
case의 구조 골격. OUT 위키의 case_pattern 노드(4종)가 원천.
_Avoid_: 템플릿, 시나리오 유형

**patternEvidence (패턴 근거)**:
승인된 위키에서 재생성되는 case_pattern의 문학적 근거 묶음—전형적 구조·단서·장소·변형과 그 출처. 원문 스냅샷이 갱신되면 다시 만들 수 있지만 게임 규칙 자체는 아니다.
_Avoid_: patternRecipe, 실행 규칙

**patternRecipe (패턴 조립법)**:
patternEvidence를 근거로 승인된, case의 실행 가능한 버전 고정 조립법—슬롯 순서·역할·의존 관계·가변축 후보를 정의한다. 새 근거는 기존 조립법을 덮어쓰지 않고 새 버전 후보를 만든다.
_Avoid_: case_pattern 원문, 템플릿, 자동 최신본

**storySeed (서사 씨앗)**:
case 생성 전에 선택하는 서사 소재와 의미 제약의 묶음. `requires`는 사건 논리·공개 순서가 성립하기 위한 필수 조건이고, `prefers`는 소재 적합성·분위기·다양성을 평가하는 선호 조건이다.
_Avoid_: story, presentation(검증된 truth를 플레이어에게 표현하는 별도 층)

**합법 후보 (legal candidate)**:
승인된 patternRecipe와 `storySeed.requires`를 만족하도록 엔진이 기존 카드·측면에서 결정론적으로 찾아낸 truth 조합. 삼중 제약과 취향 평가를 모두 통과한 emit case와는 구별한다.
_Avoid_: LLM 초안, 완성 case, 유효 case

**슬롯 해답 (slot solution)**:
truth의 한 슬롯에서 채택 가능한 단서 카드와 측면의 명시적 쌍. 상태 조건이 붙을 수 있지만, 상태 변화가 이미 확정된 해답을 사후에 오답으로 바꿔서는 안 된다.
_Avoid_: 카드 ID만인 answer, frame으로 추론한 정답 측면

**원문 스냅샷 (source snapshot)**:
계속 갱신되는 원문에서 특정 시점의 내용을 출처 식별자·수정 시각·내용 해시와 함께 고정한 생성 입력. 새 원문 버전은 기존 case를 바꾸지 않고 새 생성 후보를 만든다.
_Avoid_: 최신본, 라이브 원문

**위키 후보 (wiki candidate)**:
원문 스냅샷에서 OpenWiki나 LLM이 추출했지만 아직 OUT 정본으로 승인되지 않은 패턴·단서·story 해석. 정본 승격 전에는 generic pack과 case 생성기의 입력이 될 수 없다.
_Avoid_: 위키 페이지, 자동 생성 정본

**보스 case (복합 패턴 case)**:
case_pattern 2개를 결합해 생성된 상위 난이도 case. run의 클라이맥스.

**run (런)**:
연속된 case 여러 건(3~5건, 난이도 상승)과 마지막 보스 case로 구성되는 플레이 단위.
_Avoid_: 세션, 캠페인

**생성 제약 (삼중 제약)**:
빌드타임 case 생성물이 기계 검증으로 통과해야 하는 세 조건 — ① **풀 수 있음**: 플레이어가 그 시점에 아는 측면만으로(게스트 대여 포함) 해결 가능, 소프트락 없음 ② **이야기로 성립함**: 정답 배열의 서사 응집이 full(정답 슬롯 순서 = 서사 인과 순서) ③ **실패 방향 도달성**: 죽는 방향 각각(공개 과다·강압 과다)이 스타터 어휘만으로 도달 가능. ①②는 코어 루프 확정(03 재개정)의 이중 제약, ③은 문맥 태그 의미론 프로토타입(17)이 추가. 검증 순서는 기계 검증이 LLM 취향 필터보다 앞선다.
_Avoid_: 이중 제약(17 이후 구식), 밸런스 체크

### 카드와 수집

**단서 카드 (clue card)**:
수집 대상이자 추리 어휘. OUT 위키의 clue_type 노드가 원천. 추리 슬롯에 놓을 수 있는 유일한 재료.
_Avoid_: 단서 토큰, 증거 카드

**어휘 게이트 (vocabulary gate)**:
보유한 단서 카드만 추리에 사용할 수 있다는 규칙. 컬렉션의 성장이 풀 수 있는 case 공간을 넓힌다. 패턴 카드의 가설 선언에도 같은 규칙이 적용된다.

**가설 선언 (hypothesis declaration)**:
추리 제출 시 case의 골격 패턴을 선언하는 행위. 보유하거나 게스트로 주어진 패턴 카드만 선언할 수 있으며, 적중하면 정보 보상을 받는다.
_Avoid_: 패턴 지목, 추측

**게스트 단서 (guest clue)**:
case에 포함된 미보유 단서 유형. 그 case 안에서만 임시 사용 가능하며, 클리어 시 영구 획득된다.

**패턴 카드 (pattern card)**:
수집 대상. OUT 위키의 case_pattern 노드(4종)가 원천. 가설 선언에 쓰는 어휘로, 단서 카드와 함께 영구 컬렉션을 이룬다.
_Avoid_: 골격 카드, 시나리오 카드

**힌트 카드 (hint card)**:
소모형 카드. 근접도 힌트 등 순간 전술을 제공하며 영구 컬렉션이 아니다. 위키 비원천 — 익명적 도구 컨셉의 수제 설계.
_Avoid_: 아이템, 소모품 카드

**측면 (facet)**:
한 단서 카드가 가질 수 있는 용도 하나. `(역할, 의미)` 쌍이며, 카드는 여러 측면을 가진다 — 같은 실·섬유가 밀실에선 침입 도구, 투명인간 사건에선 신분 단서가 된다. 어느 측면이 켜지는지는 **앞 카드(이웃)가 정하고 배경 상태가 게이트한다**. 「측면」은 **표면**(側+面)이면서 **사건을 이루는 한 요소**라는 두 뜻을 함께 갖는다.
_Avoid_: 얼굴(2026-07-27까지 쓰던 말 — 생생하지만 사람 얼굴·레이든 초상과 겹쳐 검색이 안 됐다), 진상(하나뿐인 것이라 여럿을 못 담고 「틀린 진상」이 모순), 조각(추리문 텍스트 조각 `pieces`가 이미 점유), 용법, 모드, 변형

**측면의 네 상태 — 절대 한 낱말로 부르지 말 것**:
2026-07-27까지 이 셋을 모두 「잠김」으로 불러서 뜻이 서로를 지웠다. 「측면이 잠긴다」가 읽는 사람에게 안 닿은 이유가 이것이다 — 해금과 짝지어져 *"잠기면 못 쓰나?"*로 읽히는데 실제 뜻은 정반대(**쓰기로 정했다**)였다.

| 축 | 뜻 | 낱말 | 반대 | 코드 |
|---|---|---|---|---|
| 커밋 | 이 측면으로 **정했다**. 앞으로 전파된다 | **확정** | 되돌리면 풀린다(연쇄 해제) | `locked` |
| 지식 | 그 측면을 **안다**. run을 넘어 누적 | **아는 / 모르는** (노트 텍스트가 열리는 것은 **해금**) | 모르는 측면 | `knownFacets` |
| 가용성 | 배경 상태 때문에 **지금 못 쓴다**([12 §7](.scratch/case-collection/issues/12-context-tag-semantics.md) 은밀·신중 극단) | **막힘** | 열린다 | `facetStatus` |
| 판정 이력 | 그 측면으로 **제출해 결과를 봤다**. 정오와 무관 — 틀렸어도 겪은 것이다 | **판정받은 / 미판정** | 아직 안 겪음 | `note.correct !== null` |

「잠김」은 이제 **문이 잠긴 것**에만 쓴다(사건 텍스트의 밀실·온실).

네 축은 서로를 함의하지 않는다. **아는데 미판정**(공짜로 받은 `facets[0]`)과 **판정받았는데 지금 막힘**(축이 내려가 게이트가 닫힘)이 둘 다 실재하는 상태다 — 그래서 한 낱말로 부르면 안 된다.

**배치 = 확정**:
카드를 슬롯에 놓는 행위. 놓는 순간 측면이 확정되고 그 확정이 앞으로 전파되어 다음 슬롯의 해석 공간을 열고 닫는다. 되돌리면 뒤가 연쇄로 풀린다. 정답을 고르는 것이 아니라 **해석 공간을 좁히는 행위**다.
_Avoid_: 제출, 선택, **수(手)** — 2026-07-28 폐기. 바둑·장기 비유라 한국어로 뜻이 서지 않는다는 사용자 판정(플레이 중 "'수'로 읽힌다는 게 한국어 표현으로 와닿지 않음"). 커밋 축의 낱말 **확정**으로 통일해 위 3축 표와 `locked`에 맞췄다. 플레이 판정 문구는 「타격감」을 쓴다.

**노트 대조**:
집은 카드의 **판정받은** 읽기를 슬롯의 역할과 대조해, 갈 수 있는 자리를 밝히는 것. 목적은 힌트가 아니라 **헛클릭 제거**다 — 네 자리를 다 눌러 어디가 받는지 알아내는 건 플레이가 아니라 반복노동이다. 이름이 곧 규칙이다: **대조할 노트가 없으면 밝힐 것도 없다.**

말하는 것은 「읽기가 역할에 맞는가」 하나뿐이고 **「이 카드가 이 자리의 정답인가」는 말하지 않는다.** 그래서 요약을 따라도 추리는 남는다. 다면성 발견은 표기가 아니라 **놓은 뒤 피드백**에 살기 때문에 이것이 발견을 죽이지 않는다.
_Avoid_: 힌트, 가이드, 정답 표시 — 셋 다 이 기능이 하지 않는 일이다.

**수사 노트 (investigation note)**:
카드에 누적되는 검증 기록. **해금된 측면만 실제로 사용할 수 있다** — 카드를 얻으면 측면 하나는 알고, 나머지는 써보며 발견한다(보유 ≠ 그 카드의 모든 용도를 앎). 오답 해석도 줄 그어진 채 기록된다.
_Avoid_: 도감 설명, 카드 해설

**가변축 (case axis)**:
case마다 하나씩 붙는 고유 배경 상태 축. 재사용 `axisProfile` 하나를 고르고 `axisPresentation`으로 사건에 맞게 표현해, 고정축(주목·신뢰)과 함께 그 사건만의 압박을 만든다.
_Avoid_: 사건 변수, 모디파이어

**axisProfile (가변축 프로필)**:
플레이어가 여러 case에 걸쳐 익히는 6~8종의 재사용 기계 규칙—축을 미는 태그·범위·임계값·측면 gate를 정의한다. story는 기존 프로필을 고를 수 있지만 새 규칙을 즉석에서 만들 수 없다.
_Avoid_: 가변축 이름, case별 규칙

**axisPresentation (가변축 표현)**:
선택된 axisProfile을 해당 case의 서사로 보여주는 이름·양극 문구·힌트. 표현은 달라도 기계 규칙은 바꾸지 않는다.
_Avoid_: axisProfile, 모디파이어

**검증 (verified)**:
카드 발견 상태의 완성 단계 — 그 카드가 정답 확정에 포함되면 검증된다(미확인 → 검증됨). 컬렉션 진행도의 두 번째 축(보유율과 별개).
_Avoid_: 마스터, 숙련

### 인물과 표현

**레이든 (Raiden)**:
플레이어의 추리에 반응하는 탐정. 드라마투르기 반응 레이어의 목소리이자 얼굴 — 엉뚱한 배치에 몸짓으로 대꾸한다. Dead Letters에서 **이 인물 하나만** 차용했다(지도 Out of scope 개정, 2026-07-25). 세계관·플롯·다른 등장인물은 공유하지 않는다.
_Avoid_: 내레이터, 조수 (플레이어가 조수 역이다)

**태그 처리 (tag treatment)**:
카드 아트에 얹히는 태그별 시각 처리 5종(은밀·공개·논리·강압·신중). 아트는 **사물(명사)만 생성하고 태그(형용사)는 런타임에 계산**한다 — 측면이 확정되면 처리가 전이하며 바뀌므로, "배치 = 확정"이 그림으로 드러난다.
_Avoid_: 필터, 스킨

### 상태와 리스크

**배경 상태 (context state)**:
run과 case가 유지하는 세계 상태. **고정축 2개**(주목 = 은밀◁▷공개, 신뢰 = 강압◁▷신중) + **가변축 1개**로 구성된다. 놓은 카드의 태그가 트랙을 밀며(정답·오답 불문), 상태는 **어떤 측면을 쓸 수 있는지 게이트한다** — 같은 카드도 배경 상태가 다르면 의미가 달라진다. 오염 경로는 셋뿐: 카드 배치 · 되돌리기(주목↑) · 가변축(case 정의). 힌트는 자원만 소모하고 상태를 오염시키지 않는다.
_Avoid_: 월드 스테이트, 플래그

**인터루드 (interlude)**:
run에서 case와 case 사이의 이벤트 지점. 직전 case에서 쓰인 카드 태그와 배경 상태의 조합이 평가되어 이벤트를 낸다.

**BAD 엔딩**:
인터루드에서 배경 상태가 최극단에 이르러 run이 조기에 끝나는 것. 라이프·HP는 존재하지 않는다. **등급형** — 최극단만 BAD이고 그 아래는 "아프지만 생존" 구간이 경고 역할을 한다. 죽는 방향은 둘: **공개 과다(언론 재판)**, **강압 과다(수사반 붕괴)**. 은밀·신중 극단은 실패가 아니라 **측면 막힘**(쓸 수 있는 해석이 마른다). 반드시 예고된다.
_Avoid_: 게임오버, 사망
