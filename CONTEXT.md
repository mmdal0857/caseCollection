# caseCollection

퍼블릭 도메인 추리소설(OUT LLM 위키) 데이터를 기반으로, 수집한 단서 카드로 생성된 사건을 추리하는 웹 카드 게임의 도메인 언어.

## Language

### 사건과 진행

**case (사건)**:
빌드타임에 생성된 사건 퍼즐 1판 — case_pattern 골격에 story의 소재(인물·범죄유형·트롭)를 입힌 것. 플레이어가 단서 카드로 재구성하는 대상.
_Avoid_: 판, 스테이지, incident, mystery
※ OUT 프로젝트 용어집에서 "case"는 기피어(에피소드 인스턴스와 혼동)지만, 이 레포는 독립 컨텍스트로서 명시적으로 재정의해 채택한다 (코어 루프 확정 티켓, 2026-07-19).

**case_pattern (사건 패턴)**:
case의 구조 골격. OUT 위키의 case_pattern 노드(4종)가 원천.
_Avoid_: 템플릿, 시나리오 유형

**보스 case (복합 패턴 case)**:
case_pattern 2개를 결합해 생성된 상위 난이도 case. run의 클라이맥스.

**run (런)**:
연속된 case 여러 건(3~5건, 난이도 상승)과 마지막 보스 case로 구성되는 플레이 단위.
_Avoid_: 세션, 캠페인

**생성 제약 (삼중 제약)**:
빌드타임 case 생성물이 기계 검증으로 통과해야 하는 세 조건 — ① **풀 수 있음**: 플레이어가 그 시점에 아는 얼굴만으로(게스트 대여 포함) 해결 가능, 소프트락 없음 ② **이야기로 성립함**: 정답 배열의 서사 응집이 full(정답 슬롯 순서 = 서사 인과 순서) ③ **실패 방향 도달성**: 죽는 방향 각각(공개 과다·강압 과다)이 스타터 어휘만으로 도달 가능. ①②는 코어 루프 확정(03 재개정)의 이중 제약, ③은 문맥 태그 의미론 프로토타입(17)이 추가. 검증 순서는 기계 검증이 LLM 취향 필터보다 앞선다.
_Avoid_: 이중 제약(17 이후 구식), 밸런스 체크

### 카드와 수집

**단서 카드 (clue card)**:
수집 대상이자 추리 어휘. OUT 위키의 clue_type 노드가 원천. 추리 슬롯에 놓을 수 있는 유일한 재료.
_Avoid_: 단서 토큰, 증거 카드

**어휘 게이트 (vocabulary gate)**:
보유한 단서 카드만 추리에 사용할 수 있다는 규칙. 컬렉션의 성장이 풀 수 있는 case 공간을 넓힌다. 패턴 카드의 가설 선언에도 같은 규칙이 적용된다.

**가설 선언 (hypothesis declaration)**:
추리 제출 시 case의 골격 패턴을 선언하는 행위. 보유하거나 게스트로 주어진 패턴 카드만 선언할 수 있으며, 적중하면 정보 보상을 받는다.
_Avoid_: 패턴 지목, 추측

**게스트 단서 (guest clue)**:
case에 포함된 미보유 단서 유형. 그 case 안에서만 임시 사용 가능하며, 클리어 시 영구 획득된다.

**패턴 카드 (pattern card)**:
수집 대상. OUT 위키의 case_pattern 노드(4종)가 원천. 가설 선언에 쓰는 어휘로, 단서 카드와 함께 영구 컬렉션을 이룬다.
_Avoid_: 골격 카드, 시나리오 카드

**힌트 카드 (hint card)**:
소모형 카드. 근접도 힌트 등 순간 전술을 제공하며 영구 컬렉션이 아니다. 위키 비원천 — 익명적 도구 컨셉의 수제 설계.
_Avoid_: 아이템, 소모품 카드

**얼굴 (facet)**:
한 단서 카드가 가질 수 있는 용도 하나. `(역할, 의미)` 쌍이며, 카드는 여러 얼굴을 가진다 — 같은 실·섬유가 밀실에선 침입 도구, 투명인간 사건에선 신분 단서가 된다. 어느 얼굴이 켜지는지는 **앞 카드(이웃)가 정하고 배경 상태가 게이트한다**.
_Avoid_: 용법, 모드, 변형

**배치 = 수(手)**:
카드를 슬롯에 놓는 행위. 놓는 순간 얼굴이 확정·잠기고 그 확정이 앞으로 전파되어 다음 슬롯의 해석 공간을 열고 닫는다. 되돌리면 뒤가 연쇄로 풀린다. 정답을 고르는 것이 아니라 해석 공간을 좁히는 **수**다.
_Avoid_: 제출, 선택

**수사 노트 (investigation note)**:
카드에 누적되는 검증 기록. **해금된 얼굴만 실제로 사용할 수 있다** — 카드를 얻으면 얼굴 하나는 알고, 나머지는 써보며 발견한다(보유 ≠ 그 카드의 모든 용도를 앎). 오답 해석도 줄 그어진 채 기록된다.
_Avoid_: 도감 설명, 카드 해설

**가변축 (case axis)**:
case마다 하나씩 붙는 고유 배경 상태 축. 재사용 풀(6~8종)에서 선택되며 — 밀실엔 봉인, 실종엔 시간 — 그 사건만의 압박을 만든다. 고정축(주목·신뢰)과 함께 작동한다.
_Avoid_: 사건 변수, 모디파이어

**검증 (verified)**:
카드 발견 상태의 완성 단계 — 그 카드가 정답 확정에 포함되면 검증된다(미확인 → 검증됨). 컬렉션 진행도의 두 번째 축(보유율과 별개).
_Avoid_: 마스터, 숙련

### 인물과 표현

**레이든 (Raiden)**:
플레이어의 추리에 반응하는 탐정. 드라마투르기 반응 레이어의 목소리이자 얼굴 — 엉뚱한 배치에 몸짓으로 대꾸한다. Dead Letters에서 **이 인물 하나만** 차용했다(지도 Out of scope 개정, 2026-07-25). 세계관·플롯·다른 등장인물은 공유하지 않는다.
_Avoid_: 내레이터, 조수 (플레이어가 조수 역이다)

**태그 처리 (tag treatment)**:
카드 아트에 얹히는 태그별 시각 처리 5종(은밀·공개·논리·강압·신중). 아트는 **사물(명사)만 생성하고 태그(형용사)는 런타임에 계산**한다 — 얼굴이 잠기면 처리가 전이하며 바뀌므로, "배치 = 수(手)"가 그림으로 드러난다.
_Avoid_: 필터, 스킨

### 상태와 리스크

**배경 상태 (context state)**:
run과 case가 유지하는 세계 상태. **고정축 2개**(주목 = 은밀◁▷공개, 신뢰 = 강압◁▷신중) + **가변축 1개**로 구성된다. 놓은 카드의 태그가 트랙을 밀며(정답·오답 불문), 상태는 **어떤 얼굴을 쓸 수 있는지 게이트한다** — 같은 카드도 배경 상태가 다르면 의미가 달라진다. 오염 경로는 셋뿐: 카드 배치 · 되돌리기(주목↑) · 가변축(case 정의). 힌트는 자원만 소모하고 상태를 오염시키지 않는다.
_Avoid_: 월드 스테이트, 플래그

**인터루드 (interlude)**:
run에서 case와 case 사이의 이벤트 지점. 직전 case에서 쓰인 카드 태그와 배경 상태의 조합이 평가되어 이벤트를 낸다.

**BAD 엔딩**:
인터루드에서 배경 상태가 최극단에 이르러 run이 조기에 끝나는 것. 라이프·HP는 존재하지 않는다. **등급형** — 최극단만 BAD이고 그 아래는 "아프지만 생존" 구간이 경고 역할을 한다. 죽는 방향은 둘: **공개 과다(언론 재판)**, **강압 과다(수사반 붕괴)**. 은밀·신중 극단은 실패가 아니라 **얼굴 잠김**(쓸 수 있는 해석이 마른다). 반드시 예고된다.
_Avoid_: 게임오버, 사망
